{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist-server", // Where to put the compiled server JS files
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "noEmit": false
  },
  "include": ["server"] // Only compile the 'server' folder
}